%===========================================
%          Compulsory Preliminary pages & Preamble
%===========================================
%%%%% Preamble 
\usepackage{natbib}%biblography
\usepackage{color, colortbl}
\usepackage{mathtools}
\usepackage{makecell}
\usepackage{amsmath,bm}%allows the removal of newpage between chapters
\usepackage{times}%use Times New Roman
\usepackage{newtxtext,newtxmath} % makes maths Times New Roman
\DeclareMathAlphabet      {\mathbfit}{OML}{txr}{b}{it} % get bold italic maths
\usepackage{setspace}% 1.5 lines spacing!!!
%Get the correct line spacing. Note that \onehalfspacing DOES NOT work because of the way which LaTeX measures linespacing (which is different to Word). 1.3 is correct
\linespread{1.3}%\linespread{1.25}
%Make sure footnotes don't split over a page.
\interfootnotelinepenalty=10000
\usepackage{gensymb} 
\usepackage[flushleft]{threeparttable}
%align graphics to top of page
\usepackage{memhfixc}							
	%Need this to make sure the layout is correct.
\makeatletter
    \setlength\@fptop{0\p@}
\makeatother
\usepackage[table, svgnames]{xcolor}% Can modify row colours (to fake longtable)

\usepackage{uqtitle}
%Get the title page right.

% all margins 20mm 
\usepackage{geometry}
 \geometry{
 a4paper,
 left=20mm,
 top=20mm,
bottom=20mm,
right=20mm,
 }

 \usepackage[toc,page]{appendix} % appendices

% %-------------------------------------- only for show page layout
% %correct hbox overfull issues
%     \usepackage{showframe}
%     \renewcommand\ShowFrameLinethickness{0.25pt}
%     \renewcommand*\ShowFrameColor{\color{red}}
% %---------------------------------------------------------------%

%remove gap in between chapters
\usepackage{titlesec}
\titleformat{\chapter}[hang] {\normalfont\fontsize{16}{16}\bfseries\raggedright}{\chaptertitlename\ \thechapter:}{12pt}{}%{\huge}
\titlespacing*{\chapter}{0pt}{0pt}{40pt}
\titleformat{\section}[hang]{\fontsize{14}{14}\bfseries\raggedright}{\thesection}{0mm}{\MakeUppercase}
\titleformat{\subsection}[hang]{\fontsize{12}{12}\bfseries\raggedright}{\thesubsection}{0mm}{}
\titleformat{\subsubsection}[hang]{\fontsize{12}{12}\raggedright\itshape}{\thesubsubsection}{0mm}{}

%get figures to restart counting at each chapter 
%\renewcommand{\thepart}{}% removes numbering from parts
\renewcommand{\thesection}{}% removes numbering from sections
\renewcommand{\thesubsection}{}% removes numbering from subsections

%add 'Figure' and 'Table' to contents listing
\usepackage{tocloft} 
\renewcommand{\cftfigfont}{Figure }
\renewcommand{\cfttabfont}{Table }
\newlength\mylenfig
\settowidth\mylenfig{A3.S3.}
\cftsetindents{figure}{\mylenfig}{\mylenfig}
\cftsetindents{table}{\mylenfig}{\mylenfig}


%add 'Chapter' to thesis contents
\renewcommand\cftchappresnum{Chapter }
\renewcommand\cftchapaftersnum{.}
\newlength\mylen
\settowidth\mylen{Chapter 1. ~}
\cftsetindents{chap}{0pt}{\mylen}

%remove titles from tocs and lofs
\makeatletter
\renewcommand{\@cftmaketoctitle}{}
\makeatother

\makeatletter%list of figures
\renewcommand{\@cftmakeloftitle}{}
\makeatother

\makeatletter%list of tables
\renewcommand{\@cftmakelottitle}{}
\makeatother

%adjust indentation for toc
\setlength{\cftsecindent}{3.1em}
\setlength{\cftsubsecindent}{4em}
\renewcommand{\cftsubsecfont}{\itshape}
% \setlength{\cftsubsubsecindent}{0em}
\setlength{\cftfigindent}{0pt}  % remove indentation from figures in lof
\setlength{\cfttabindent}{0pt}  % remove indentation from tables in lot
\renewcommand{\cftfigpresnum}{~}
\addtolength{\cftfignumwidth}{4em}
\renewcommand{\cftfigaftersnumb}{\hspace*{-3.75em}}
\renewcommand{\cfttabpresnum}{~}
\addtolength{\cfttabnumwidth}{4em}
\renewcommand{\cfttabaftersnumb}{\hspace*{-3.75em}}

%fancy headers and footers
% \usepackage{fancyhdr}   
% \pagestyle{fancy}
% \fancyhead{}% clear headers
% \renewcommand{\headrulewidth}{0pt}
% \fancyfoot[C]{\thepage} % except the center


% code for aligning bold decimal points
\usepackage{booktabs}
\usepackage[detect-all]{siunitx}
\sisetup{input-decimal-markers={.}}
\usepackage{newtxtext,newtxmath}
%allows the bm maths to be aligned
\newrobustcmd*{\bftabnum}{%
  \bfseries
  \sisetup{output-decimal-marker={\textmd{.}}}%
}

\usepackage{multirow}
\usepackage{caption}
\usepackage[labelfont=bf]{caption}
\usepackage[labelsep=period]{caption}
\captionsetup{singlelinecheck=false,font={stretch=1.3}, compatibility=false}% get the fig/tab captions to align left % justification=raggedright,
% \newcommand\mycaption[2]{\caption[#1]{#1. #2}}
%fix the captions and caption list make the first sentence bold
% \usepackage{graphicx} % used for subfigures as well
% \let\oldcaption\caption
% \renewcommand{\caption}[2]{
%     \oldcaption[#1]{\textbf{#1} #2}
% }
\usepackage{array, tabularx}
\newcolumntype {+}{ >{\global\let\currentrowstyle\relax }}
\newcolumntype {^}{ >{\currentrowstyle }}
\newcommand {\rowstyle }[1]{\gdef\currentrowstyle{#1}% 
  #1\ignorespaces
  }



%======New Packages ==========%
\usepackage{etoolbox} %allows urls
\usepackage{url} %allows urls
\usepackage{hyperref}
\usepackage[table]{xcolor} %allows tables to have alternate grey shading
\usepackage{rotating}% rotates images
\usepackage{placeins}%force float barriers
\usepackage{afterpage} %force float barriers
\usepackage{pdflscape}% landscape
\usepackage{array}%allows centring on a fixed width margin
\newcolumntype{C}[1]{>{\centering\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}
\usepackage{subfig}% setup caption for subcaption


% redefine \raggedright to include an indent for each new paragraph
\makeatletter
\newcommand\iraggedright{%
  \let\\\@centercr\@rightskip\@flushglue \rightskip\@rightskip
  \leftskip\z@skip}
\makeatother
%\usepackage{ragged2e}
%\setlength{\RaggedRightParindent}{\parindent}

%add a line break between paragraphs
\usepackage{titlesec}
% \setlength{\parindent}{0pt}%removes or set paragraph indentation
\setlength{\parskip}{12pt plus 6pt minus 4pt}

%Settings for the headings
\titlespacing*{\subsection}{0pt}{\parskip}{0.05pt plus 4pt minus 2pt}
\titlespacing*{\subsubsection}{0pt}{\parskip}{0.05pt plus 4pt minus 2pt}


\usepackage{xcolor}% allows text colouring
 \usepackage[normalem]{ulem}
 \useunder{\uline}{\ul}{}
\usepackage[flushleft]{threeparttable}% allows footnotes
\renewcommand{\vec}[1]{\mathbf{#1}}% in conjunction with amsmath, bolds but unitalicizes matrices and vectors

\usepackage{epstopdf-base}% allows the eps as main image to be declared
%======USE KABLE LONGTABLE generated in R ==========%
\usepackage{longtable}
%\usepackage{array}
% \usepackage{multirow}
% \usepackage{wrapfig}
%\usepackage{float}
%\usepackage{colortbl}
%\usepackage{tabu}
%\usepackage{threeparttable}
%\usepackage{threeparttablex}
%\usepackage[normalem]{ulem}
%\usepackage{makecell}


%preamble for VM estimates SUPP
\usepackage{bibentry}% allows full in-text citations

% black references, blue urls and black citations
\hypersetup{colorlinks=true,linkcolor=black,citecolor=black,urlcolor=blue}
% get a blue underline